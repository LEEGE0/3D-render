# 프로젝트 작업 규칙

이 문서는 이 저장소에서 작업하는 모든 자동화 에이전트와 개발자가 따라야 하는 규칙이다. 하위 폴더에 더 구체적인 `AGENTS.md`가 있으면 그 문서가 해당 범위에서 우선한다.

## 기본 환경

- 대상 운영체제는 Windows 11 전용이다.
- 프로젝트, 엔진, 외부 도구, 캐시, 추출 자산과 렌더링 결과는 원칙적으로 `D:\3D-render` 아래에서 관리한다.
- 실행 프로그램은 오프라인 독립 실행을 기본으로 하며 런타임 네트워크 의존성, 원격 분석, 자동 업로드를 추가하지 않는다.
- 애플리케이션 구현은 Godot 4.7.2 Stable .NET, C#, Forward+를 기준으로 한다.
- 게임 원본 파일은 읽기 전용으로 취급한다. 필요한 파일은 `local-assets/`로 복사한 뒤 사본만 분석한다.
- 저작권이 있는 게임 원본·추출 자산, 대용량 도구, 캐시 및 렌더 출력은 Git에 포함하지 않는다.

## 변경 및 검증

- 사용자의 기존 변경을 보존하고, 작업 범위와 무관한 파일은 수정하거나 스테이징하지 않는다.
- 완료를 주장하기 전에 해당 변경을 증명하는 최신 검증 명령을 실행하고 결과를 확인한다.
- 코드 기능은 가능한 한 테스트를 먼저 작성하고 실패를 확인한 다음 최소 구현과 회귀 검증 순서로 진행한다.
- 문서 변경은 링크, 파일 존재 여부, 금지된 임시 문구(`TBD`, `TODO`)와 Git 추적 범위를 확인한다.
- 오류가 발생하면 원인을 확인하기 전에 임의의 우회 수정이나 반복 설치를 하지 않는다.

## 커밋과 푸시

- 하나의 작업 단위가 끝날 때마다 반드시 커밋하고 원격 저장소에 푸시한다.
- 커밋 전 `git status`, staged/unstaged diff, 추적 대상 파일을 확인한다.
- 스테이징은 `git add -- <확인된 경로>`처럼 명시한 경로에만 수행한다. `git add .`, `git add -A`, `git add --all`은 사용하지 않는다.
- 커밋 메시지는 변경 목적을 한눈에 알 수 있게 작성한다. 예: `docs: DSR 애니메이션 자산 조사 기록`.
- 현재 기능 브랜치를 원격에 푸시하고 upstream을 설정한다. 사용자의 명시적 요청 없이 강제 푸시하지 않는다.
- 푸시 실패 시 인증, 원격 주소, 브랜치와 네트워크 상태를 진단하고 실제 실패 상태를 사용자에게 알린다.

## 정상 동작 버전 태그

1. 사용자가 “잘 된다”, “정상 동작한다”, “문제가 해결됐다” 또는 같은 의미의 말을 하면 즉시 태그하지 않는다.
2. 어떤 기능과 시나리오가 정상인지 구체적으로 적어 사용자에게 재확인한다.
3. 사용자가 재확인에 긍정적으로 응답한 경우에만 현재 변경을 검증하고 커밋·푸시한다.
4. 그 커밋에 주석 태그를 생성한다. 태그 이름은 `working/<기능-슬러그>-YYYYMMDD-HHmm` 형식을 사용하고, 태그 메시지에 정상 동작 기능, 확인 시나리오, 사용자 확인 일시를 한글로 기록한다.
5. `git push origin <태그>`로 태그를 원격에 푸시하고 커밋 SHA와 태그 이름을 사용자에게 알린다.
6. 이미 같은 기능·커밋을 가리키는 적절한 태그가 있으면 중복 태그를 만들지 않고 기존 태그를 안내한다.

## 자산과 보안

- `local-assets/`, `tools/`, `cache/`, `exports/`는 로컬 전용이다.
- 게임 설치 폴더와 Steam 파일을 수정·덮어쓰기·재패킹하지 않는다.
- 비밀키, 토큰, 계정 정보와 개인 경로를 문서나 커밋에 기록하지 않는다.
- 외부 도구를 받을 때는 공식 배포처를 우선하고 버전, 다운로드 주소, 체크섬을 조사 문서에 기록한다.
- 프로젝트가 배포되더라도 게임 자산은 포함하지 않으며 사용자가 합법적으로 보유한 로컬 설치에서 직접 가져오도록 한다.
